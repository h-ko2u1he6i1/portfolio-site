# Basic authentication for the whole site (see src/proxy.ts).
# Set these in the Vercel dashboard for Production and Preview.
# If either is empty, the site is served without authentication.
BASIC_AUTH_USER=
BASIC_AUTH_PASSWORD=

# Canonical site URL, used for metadata / sitemap / robots.
NEXT_PUBLIC_SITE_URL=https://your-domain.example
